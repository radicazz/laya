# LeakSanitizer suppression file for Laya
# Suppress known false positives from external libraries

# SDL3 X11/Wayland backend leaks - these are in system libraries SDL uses
# X11 keyboard initialization
leak:X11_InitKeyboard
leak:X11_VideoInit

# SDL video and subsystem initialization
leak:SDL_VideoInit
leak:SDL_InitSubSystem

# X11 library internal allocations
leak:libX11
leak:libxcb

# GTK/GDK/libdecor leaks in SDL3 Wayland backend
leak:libglib-2.0.so
leak:libgdk-3.so
leak:libdecor-gtk.so
leak:libdecor-0.so

